// ─────────────────────────────────────────────────────────────────────────────
// 符譜 · Fupu —— 逐符之式與出典（本庫之「儀軌」）
// 律：寧缺毋誤——式・出典・結構・用途四字段 ≥2 源乃「已核」；
// 網傳「靈符」不錄；祕傳者記位注「祕傳不錄」。
// ─────────────────────────────────────────────────────────────────────────────
export type 符類 = '入山符' | '真形圖' | '複文' | '鎮宅符' | '雷法符' | '籙' | '其他';
export type 信級 = '已核' | '待核';

export interface 出典條 { 典: string; 據?: string; 引?: string; 註?: string; 卷?: string }

export interface 符條 {
  id: string;                 // ASCII 符號（庫鍵）
  名: string;                 // 符名
  類: 符類;
  式: string | null;          // 結構之述（符頭/身/膽/腳各何物）——查不實置 null
  用: string | null;          // 用途（典載之用，非勸用——研究陳列）
  出典: 出典條[];
  異說?: string;
  信: 信級;
}

export const 符譜: 符條[] = [
  // 考據先行，勿先填——見 docs/考據/（立卷後逐條入此）
];

export const 依號: Record<string, 符條> = Object.fromEntries(符譜.map(f => [f.id, f]));
